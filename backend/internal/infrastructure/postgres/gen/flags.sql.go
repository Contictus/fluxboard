// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: flags.sql

package gen

import (
	"context"

	"github.com/google/uuid"
)

const listFeatureFlags = `-- name: ListFeatureFlags :many

SELECT org_id, flag, enabled FROM feature_flags
WHERE org_id = $1
ORDER BY flag
`

// Feature flags — Phase 6 (FR-ADM-006). feature_flags is [T] (RLS via TenantPool).
func (q *Queries) ListFeatureFlags(ctx context.Context, orgID uuid.UUID) ([]FeatureFlag, error) {
	rows, err := q.db.Query(ctx, listFeatureFlags, orgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []FeatureFlag
	for rows.Next() {
		var i FeatureFlag
		if err := rows.Scan(&i.OrgID, &i.Flag, &i.Enabled); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertFeatureFlag = `-- name: UpsertFeatureFlag :exec
INSERT INTO feature_flags (org_id, flag, enabled)
VALUES ($1, $2, $3)
ON CONFLICT (org_id, flag) DO UPDATE SET enabled = EXCLUDED.enabled
`

type UpsertFeatureFlagParams struct {
	OrgID   uuid.UUID `json:"org_id"`
	Flag    string    `json:"flag"`
	Enabled bool      `json:"enabled"`
}

func (q *Queries) UpsertFeatureFlag(ctx context.Context, arg UpsertFeatureFlagParams) error {
	_, err := q.db.Exec(ctx, upsertFeatureFlag, arg.OrgID, arg.Flag, arg.Enabled)
	return err
}
