// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: task_links.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const createTaskLink = `-- name: CreateTaskLink :exec

INSERT INTO task_links (task_id, linked_task_id, org_id)
VALUES ($1, $2, $3)
ON CONFLICT (task_id, linked_task_id) DO NOTHING
`

type CreateTaskLinkParams struct {
	TaskID       uuid.UUID `json:"task_id"`
	LinkedTaskID uuid.UUID `json:"linked_task_id"`
	OrgID        uuid.UUID `json:"org_id"`
}

// Task links ([T], tenant-scoped, FR-LINKS) ----------------------------------
func (q *Queries) CreateTaskLink(ctx context.Context, arg CreateTaskLinkParams) error {
	_, err := q.db.Exec(ctx, createTaskLink, arg.TaskID, arg.LinkedTaskID, arg.OrgID)
	return err
}

const deleteTaskLink = `-- name: DeleteTaskLink :execrows
DELETE FROM task_links
WHERE org_id = $1 AND task_id = $2 AND linked_task_id = $3
`

type DeleteTaskLinkParams struct {
	OrgID        uuid.UUID `json:"org_id"`
	TaskID       uuid.UUID `json:"task_id"`
	LinkedTaskID uuid.UUID `json:"linked_task_id"`
}

func (q *Queries) DeleteTaskLink(ctx context.Context, arg DeleteTaskLinkParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteTaskLink, arg.OrgID, arg.TaskID, arg.LinkedTaskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const listTaskBlocked = `-- name: ListTaskBlocked :many
SELECT t.id, t.org_id, t.project_id, t.column_id, t.number, t.title, t.created_at
FROM task_links tl
JOIN tasks t ON t.id = tl.task_id
WHERE tl.org_id = $1 AND tl.linked_task_id = $2 AND t.deleted_at IS NULL
ORDER BY t.number
`

type ListTaskBlockedParams struct {
	OrgID  uuid.UUID `json:"org_id"`
	TaskID uuid.UUID `json:"task_id"`
}

type ListTaskBlockedRow struct {
	ID        uuid.UUID `json:"id"`
	OrgID     uuid.UUID `json:"org_id"`
	ProjectID uuid.UUID `json:"project_id"`
	ColumnID  uuid.UUID `json:"column_id"`
	Number    int32     `json:"number"`
	Title     string    `json:"title"`
	CreatedAt time.Time `json:"created_at"`
}

func (q *Queries) ListTaskBlocked(ctx context.Context, arg ListTaskBlockedParams) ([]ListTaskBlockedRow, error) {
	rows, err := q.db.Query(ctx, listTaskBlocked, arg.OrgID, arg.TaskID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTaskBlockedRow
	for rows.Next() {
		var i ListTaskBlockedRow
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ProjectID,
			&i.ColumnID,
			&i.Number,
			&i.Title,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTaskBlockers = `-- name: ListTaskBlockers :many
SELECT t.id, t.org_id, t.project_id, t.column_id, t.number, t.title, t.created_at
FROM task_links tl
JOIN tasks t ON t.id = tl.linked_task_id
WHERE tl.org_id = $1 AND tl.task_id = $2 AND t.deleted_at IS NULL
ORDER BY t.number
`

type ListTaskBlockersParams struct {
	OrgID  uuid.UUID `json:"org_id"`
	TaskID uuid.UUID `json:"task_id"`
}

type ListTaskBlockersRow struct {
	ID        uuid.UUID `json:"id"`
	OrgID     uuid.UUID `json:"org_id"`
	ProjectID uuid.UUID `json:"project_id"`
	ColumnID  uuid.UUID `json:"column_id"`
	Number    int32     `json:"number"`
	Title     string    `json:"title"`
	CreatedAt time.Time `json:"created_at"`
}

func (q *Queries) ListTaskBlockers(ctx context.Context, arg ListTaskBlockersParams) ([]ListTaskBlockersRow, error) {
	rows, err := q.db.Query(ctx, listTaskBlockers, arg.OrgID, arg.TaskID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTaskBlockersRow
	for rows.Next() {
		var i ListTaskBlockersRow
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ProjectID,
			&i.ColumnID,
			&i.Number,
			&i.Title,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
