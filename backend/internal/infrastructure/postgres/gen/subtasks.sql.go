// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: subtasks.sql

package gen

import (
	"context"

	"github.com/google/uuid"
)

const countSubtasksForProject = `-- name: CountSubtasksForProject :many
SELECT s.task_id, COUNT(*) AS total, COUNT(*) FILTER (WHERE s.done) AS done
FROM subtasks s
JOIN tasks t ON t.id = s.task_id
WHERE s.org_id = $1 AND t.project_id = $2
GROUP BY s.task_id
`

type CountSubtasksForProjectParams struct {
	OrgID     uuid.UUID `json:"org_id"`
	ProjectID uuid.UUID `json:"project_id"`
}

type CountSubtasksForProjectRow struct {
	TaskID uuid.UUID `json:"task_id"`
	Total  int64     `json:"total"`
	Done   int64     `json:"done"`
}

func (q *Queries) CountSubtasksForProject(ctx context.Context, arg CountSubtasksForProjectParams) ([]CountSubtasksForProjectRow, error) {
	rows, err := q.db.Query(ctx, countSubtasksForProject, arg.OrgID, arg.ProjectID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CountSubtasksForProjectRow
	for rows.Next() {
		var i CountSubtasksForProjectRow
		if err := rows.Scan(&i.TaskID, &i.Total, &i.Done); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const createSubtask = `-- name: CreateSubtask :exec

INSERT INTO subtasks (id, org_id, task_id, title, done, rank)
VALUES ($1, $2, $3, $4, $5, $6)
`

type CreateSubtaskParams struct {
	ID     uuid.UUID `json:"id"`
	OrgID  uuid.UUID `json:"org_id"`
	TaskID uuid.UUID `json:"task_id"`
	Title  string    `json:"title"`
	Done   bool      `json:"done"`
	Rank   string    `json:"rank"`
}

// Subtasks ([T], tenant-scoped, FR-TASK-003) --------------------------------
func (q *Queries) CreateSubtask(ctx context.Context, arg CreateSubtaskParams) error {
	_, err := q.db.Exec(ctx, createSubtask,
		arg.ID,
		arg.OrgID,
		arg.TaskID,
		arg.Title,
		arg.Done,
		arg.Rank,
	)
	return err
}

const deleteSubtask = `-- name: DeleteSubtask :execrows
DELETE FROM subtasks WHERE org_id = $1 AND id = $2
`

type DeleteSubtaskParams struct {
	OrgID uuid.UUID `json:"org_id"`
	ID    uuid.UUID `json:"id"`
}

func (q *Queries) DeleteSubtask(ctx context.Context, arg DeleteSubtaskParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteSubtask, arg.OrgID, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getSubtask = `-- name: GetSubtask :one
SELECT id, org_id, task_id, title, done, rank, created_at, updated_at
FROM subtasks
WHERE org_id = $1 AND id = $2
`

type GetSubtaskParams struct {
	OrgID uuid.UUID `json:"org_id"`
	ID    uuid.UUID `json:"id"`
}

func (q *Queries) GetSubtask(ctx context.Context, arg GetSubtaskParams) (Subtask, error) {
	row := q.db.QueryRow(ctx, getSubtask, arg.OrgID, arg.ID)
	var i Subtask
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.TaskID,
		&i.Title,
		&i.Done,
		&i.Rank,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listSubtasksByTask = `-- name: ListSubtasksByTask :many
SELECT id, org_id, task_id, title, done, rank, created_at, updated_at
FROM subtasks
WHERE org_id = $1 AND task_id = $2
ORDER BY rank
`

type ListSubtasksByTaskParams struct {
	OrgID  uuid.UUID `json:"org_id"`
	TaskID uuid.UUID `json:"task_id"`
}

func (q *Queries) ListSubtasksByTask(ctx context.Context, arg ListSubtasksByTaskParams) ([]Subtask, error) {
	rows, err := q.db.Query(ctx, listSubtasksByTask, arg.OrgID, arg.TaskID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Subtask
	for rows.Next() {
		var i Subtask
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.TaskID,
			&i.Title,
			&i.Done,
			&i.Rank,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateSubtask = `-- name: UpdateSubtask :execrows
UPDATE subtasks SET title = $1, done = $2
WHERE org_id = $3 AND id = $4
`

type UpdateSubtaskParams struct {
	Title string    `json:"title"`
	Done  bool      `json:"done"`
	OrgID uuid.UUID `json:"org_id"`
	ID    uuid.UUID `json:"id"`
}

func (q *Queries) UpdateSubtask(ctx context.Context, arg UpdateSubtaskParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateSubtask,
		arg.Title,
		arg.Done,
		arg.OrgID,
		arg.ID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
