// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: sprints.sql

package gen

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const clearProjectSprints = `-- name: ClearProjectSprints :execrows
UPDATE tasks SET sprint_id = NULL
WHERE org_id = $1 AND project_id = $2 AND sprint_id = $3
  AND deleted_at IS NULL
`

type ClearProjectSprintsParams struct {
	OrgID     uuid.UUID   `json:"org_id"`
	ProjectID uuid.UUID   `json:"project_id"`
	SprintID  pgtype.UUID `json:"sprint_id"`
}

func (q *Queries) ClearProjectSprints(ctx context.Context, arg ClearProjectSprintsParams) (int64, error) {
	result, err := q.db.Exec(ctx, clearProjectSprints, arg.OrgID, arg.ProjectID, arg.SprintID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const createSprint = `-- name: CreateSprint :exec

INSERT INTO sprints (id, org_id, project_id, name, goal, status, started_at, ended_at, created_by)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
`

type CreateSprintParams struct {
	ID        uuid.UUID          `json:"id"`
	OrgID     uuid.UUID          `json:"org_id"`
	ProjectID uuid.UUID          `json:"project_id"`
	Name      string             `json:"name"`
	Goal      string             `json:"goal"`
	Status    string             `json:"status"`
	StartedAt pgtype.Timestamptz `json:"started_at"`
	EndedAt   pgtype.Timestamptz `json:"ended_at"`
	CreatedBy uuid.UUID          `json:"created_by"`
}

// Sprints ([T], tenant-scoped, FR-SPRINT) ----------------------------------
func (q *Queries) CreateSprint(ctx context.Context, arg CreateSprintParams) error {
	_, err := q.db.Exec(ctx, createSprint,
		arg.ID,
		arg.OrgID,
		arg.ProjectID,
		arg.Name,
		arg.Goal,
		arg.Status,
		arg.StartedAt,
		arg.EndedAt,
		arg.CreatedBy,
	)
	return err
}

const deleteSprint = `-- name: DeleteSprint :execrows
DELETE FROM sprints
WHERE org_id = $1 AND id = $2
`

type DeleteSprintParams struct {
	OrgID uuid.UUID `json:"org_id"`
	ID    uuid.UUID `json:"id"`
}

func (q *Queries) DeleteSprint(ctx context.Context, arg DeleteSprintParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteSprint, arg.OrgID, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getActiveSprint = `-- name: GetActiveSprint :one
SELECT id, org_id, project_id, name, goal, status, started_at, ended_at,
       completed_total, completed_done, created_by, created_at
FROM sprints
WHERE org_id = $1 AND project_id = $2 AND status = 'active'
LIMIT 1
`

type GetActiveSprintParams struct {
	OrgID     uuid.UUID `json:"org_id"`
	ProjectID uuid.UUID `json:"project_id"`
}

func (q *Queries) GetActiveSprint(ctx context.Context, arg GetActiveSprintParams) (Sprint, error) {
	row := q.db.QueryRow(ctx, getActiveSprint, arg.OrgID, arg.ProjectID)
	var i Sprint
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.Name,
		&i.Goal,
		&i.Status,
		&i.StartedAt,
		&i.EndedAt,
		&i.CompletedTotal,
		&i.CompletedDone,
		&i.CreatedBy,
		&i.CreatedAt,
	)
	return i, err
}

const getSprint = `-- name: GetSprint :one
SELECT id, org_id, project_id, name, goal, status, started_at, ended_at,
       completed_total, completed_done, created_by, created_at
FROM sprints
WHERE org_id = $1 AND id = $2
`

type GetSprintParams struct {
	OrgID uuid.UUID `json:"org_id"`
	ID    uuid.UUID `json:"id"`
}

func (q *Queries) GetSprint(ctx context.Context, arg GetSprintParams) (Sprint, error) {
	row := q.db.QueryRow(ctx, getSprint, arg.OrgID, arg.ID)
	var i Sprint
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ProjectID,
		&i.Name,
		&i.Goal,
		&i.Status,
		&i.StartedAt,
		&i.EndedAt,
		&i.CompletedTotal,
		&i.CompletedDone,
		&i.CreatedBy,
		&i.CreatedAt,
	)
	return i, err
}

const listSprintsByProject = `-- name: ListSprintsByProject :many
SELECT id, org_id, project_id, name, goal, status, started_at, ended_at,
       completed_total, completed_done, created_by, created_at
FROM sprints
WHERE org_id = $1 AND project_id = $2
ORDER BY created_at
`

type ListSprintsByProjectParams struct {
	OrgID     uuid.UUID `json:"org_id"`
	ProjectID uuid.UUID `json:"project_id"`
}

func (q *Queries) ListSprintsByProject(ctx context.Context, arg ListSprintsByProjectParams) ([]Sprint, error) {
	rows, err := q.db.Query(ctx, listSprintsByProject, arg.OrgID, arg.ProjectID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Sprint
	for rows.Next() {
		var i Sprint
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ProjectID,
			&i.Name,
			&i.Goal,
			&i.Status,
			&i.StartedAt,
			&i.EndedAt,
			&i.CompletedTotal,
			&i.CompletedDone,
			&i.CreatedBy,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setTaskSprint = `-- name: SetTaskSprint :execrows
UPDATE tasks SET sprint_id = $1
WHERE org_id = $2 AND id = $3 AND deleted_at IS NULL
`

type SetTaskSprintParams struct {
	SprintID pgtype.UUID `json:"sprint_id"`
	OrgID    uuid.UUID   `json:"org_id"`
	ID       uuid.UUID   `json:"id"`
}

func (q *Queries) SetTaskSprint(ctx context.Context, arg SetTaskSprintParams) (int64, error) {
	result, err := q.db.Exec(ctx, setTaskSprint, arg.SprintID, arg.OrgID, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateSprint = `-- name: UpdateSprint :execrows
UPDATE sprints
SET name = $1, goal = $2, status = $3,
    started_at = $4, ended_at = $5,
    completed_total = $6, completed_done = $7
WHERE org_id = $8 AND id = $9
`

type UpdateSprintParams struct {
	Name           string             `json:"name"`
	Goal           string             `json:"goal"`
	Status         string             `json:"status"`
	StartedAt      pgtype.Timestamptz `json:"started_at"`
	EndedAt        pgtype.Timestamptz `json:"ended_at"`
	CompletedTotal int32              `json:"completed_total"`
	CompletedDone  int32              `json:"completed_done"`
	OrgID          uuid.UUID          `json:"org_id"`
	ID             uuid.UUID          `json:"id"`
}

func (q *Queries) UpdateSprint(ctx context.Context, arg UpdateSprintParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateSprint,
		arg.Name,
		arg.Goal,
		arg.Status,
		arg.StartedAt,
		arg.EndedAt,
		arg.CompletedTotal,
		arg.CompletedDone,
		arg.OrgID,
		arg.ID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
