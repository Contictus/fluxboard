// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: time_entries.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createTimeEntry = `-- name: CreateTimeEntry :exec

INSERT INTO time_entries (id, org_id, task_id, user_id, started_at, ended_at, note)
VALUES ($1, $2, $3, $4, $5, $6, $7)
`

type CreateTimeEntryParams struct {
	ID        uuid.UUID          `json:"id"`
	OrgID     uuid.UUID          `json:"org_id"`
	TaskID    uuid.UUID          `json:"task_id"`
	UserID    uuid.UUID          `json:"user_id"`
	StartedAt time.Time          `json:"started_at"`
	EndedAt   pgtype.Timestamptz `json:"ended_at"`
	Note      string             `json:"note"`
}

// Time entries ([T], tenant-scoped, FR-TIME) -------------------------------
func (q *Queries) CreateTimeEntry(ctx context.Context, arg CreateTimeEntryParams) error {
	_, err := q.db.Exec(ctx, createTimeEntry,
		arg.ID,
		arg.OrgID,
		arg.TaskID,
		arg.UserID,
		arg.StartedAt,
		arg.EndedAt,
		arg.Note,
	)
	return err
}

const deleteTimeEntry = `-- name: DeleteTimeEntry :execrows
DELETE FROM time_entries
WHERE org_id = $1 AND id = $2
`

type DeleteTimeEntryParams struct {
	OrgID uuid.UUID `json:"org_id"`
	ID    uuid.UUID `json:"id"`
}

func (q *Queries) DeleteTimeEntry(ctx context.Context, arg DeleteTimeEntryParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteTimeEntry, arg.OrgID, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getTimeEntry = `-- name: GetTimeEntry :one
SELECT id, org_id, task_id, user_id, started_at, ended_at, note, created_at
FROM time_entries
WHERE org_id = $1 AND id = $2
`

type GetTimeEntryParams struct {
	OrgID uuid.UUID `json:"org_id"`
	ID    uuid.UUID `json:"id"`
}

func (q *Queries) GetTimeEntry(ctx context.Context, arg GetTimeEntryParams) (TimeEntry, error) {
	row := q.db.QueryRow(ctx, getTimeEntry, arg.OrgID, arg.ID)
	var i TimeEntry
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.TaskID,
		&i.UserID,
		&i.StartedAt,
		&i.EndedAt,
		&i.Note,
		&i.CreatedAt,
	)
	return i, err
}

const listRunningTimeEntries = `-- name: ListRunningTimeEntries :many
SELECT id, org_id, task_id, user_id, started_at, ended_at, note, created_at
FROM time_entries
WHERE org_id = $1 AND user_id = $2 AND ended_at IS NULL
ORDER BY started_at DESC
`

type ListRunningTimeEntriesParams struct {
	OrgID  uuid.UUID `json:"org_id"`
	UserID uuid.UUID `json:"user_id"`
}

func (q *Queries) ListRunningTimeEntries(ctx context.Context, arg ListRunningTimeEntriesParams) ([]TimeEntry, error) {
	rows, err := q.db.Query(ctx, listRunningTimeEntries, arg.OrgID, arg.UserID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []TimeEntry
	for rows.Next() {
		var i TimeEntry
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.TaskID,
			&i.UserID,
			&i.StartedAt,
			&i.EndedAt,
			&i.Note,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTimeEntriesByTask = `-- name: ListTimeEntriesByTask :many
SELECT id, org_id, task_id, user_id, started_at, ended_at, note, created_at
FROM time_entries
WHERE org_id = $1 AND task_id = $2
ORDER BY started_at DESC
`

type ListTimeEntriesByTaskParams struct {
	OrgID  uuid.UUID `json:"org_id"`
	TaskID uuid.UUID `json:"task_id"`
}

func (q *Queries) ListTimeEntriesByTask(ctx context.Context, arg ListTimeEntriesByTaskParams) ([]TimeEntry, error) {
	rows, err := q.db.Query(ctx, listTimeEntriesByTask, arg.OrgID, arg.TaskID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []TimeEntry
	for rows.Next() {
		var i TimeEntry
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.TaskID,
			&i.UserID,
			&i.StartedAt,
			&i.EndedAt,
			&i.Note,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const stopTimeEntry = `-- name: StopTimeEntry :execrows
UPDATE time_entries SET ended_at = $1
WHERE org_id = $2 AND id = $3 AND ended_at IS NULL
`

type StopTimeEntryParams struct {
	EndedAt pgtype.Timestamptz `json:"ended_at"`
	OrgID   uuid.UUID          `json:"org_id"`
	ID      uuid.UUID          `json:"id"`
}

func (q *Queries) StopTimeEntry(ctx context.Context, arg StopTimeEntryParams) (int64, error) {
	result, err := q.db.Exec(ctx, stopTimeEntry, arg.EndedAt, arg.OrgID, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
